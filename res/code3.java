package com.github.oxaoo.lexer;

import ssdfsdf;
import sdsd.sdfvs.sgdsdf;

/**
 * Created by dydus on 03/11/2015.
 */
enum TestEnum {
    CASE1, CASE2
};

public class TempClass {
    public int main(String[] args) {
        int counter = 3;
        do {
            counter++;
            do {
                counter++;
                do {
                    counter++;
                } while(counter != 10);
            } while(counter != 10);
        } while(counter != 10);
        double a = 19.0;
        short as = 1;
        boolean n = true;
        /*
            sdvohhdsv
         */
        TestEnum st;
        st = TestEnum.CASE1;

        return counter;
    }
}
