package com.github.oxaoo.lexer;

import ssdfsdf;

public class TempClass {

    public int main() {
        double variable = 0;
        do {
            variable = variable + 10 * 25;
        } while(variable <= 100);
        //while(1000 >= variable);
        return 0;
    }
}
